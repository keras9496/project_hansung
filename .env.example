ADMIN_TOKEN=change-me
DB_PATH=data/reservations.db
TIMEZONE=Asia/Seoul

# ── Claude (Anthropic API) ──
# 키가 있으면 자동으로 live, 없으면 mock 으로 동작.
# 로컬 시연 시 키 없이 mock 으로도 모든 UI 가 작동.
ANTHROPIC_API_KEY=
LLM_MODE=auto          # auto | live | mock
CLAUDE_MODEL=claude-opus-4-7
